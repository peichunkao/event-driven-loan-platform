POSTGRES_PORT=5432
POSTGRES_USER=loan
POSTGRES_PASSWORD=loan
POSTGRES_DB=loan

# Kafka-compatible API (Redpanda): host clients use localhost:9092; containers use redpanda:29092
KAFKA_BROKERS=localhost:9092

# Used by host-run API / workers (not by compose service names)
DATABASE_URL=postgres://loan:loan@127.0.0.1:5432/loan
KAFKA_BROKERS=localhost:9092
OPENSEARCH_URL=http://localhost:9200
